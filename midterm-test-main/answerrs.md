## 1. SQL Injection กับ Allowlist

การใช้ Allowlist สำหรับตรวจสอบชื่อคอลัมน์ที่ใช้ Sort มีความสำคัญ เพราะชื่อคอลัมน์ไม่สามารถใช้ `?` ของ SQL Parameter ได้เหมือนค่าข้อมูลทั่วไป หากนำค่าจาก `req.query.sortBy` ไปต่อใน SQL โดยตรง อาจทำให้ผู้โจมตีใส่คำสั่ง SQL ที่ไม่ต้องการเข้ามาได้ ดังนั้นจึงต้องกำหนดรายการคอลัมน์ที่อนุญาต เช่น `course_name`, `credit` และ `created_at` แล้วตรวจสอบก่อนนำไปใช้ใน SQL เพื่อช่วยป้องกัน SQL Injection

## 2. ความสำคัญของ Transaction

Transaction มีความสำคัญเมื่อมีการเขียนข้อมูลหลายตารางที่เกี่ยวข้องกัน เช่น การเพิ่มข้อมูล `courses` และ `course_prerequisites` เพราะต้องการให้การทำงานทั้งหมดสำเร็จพร้อมกัน หากขั้นตอนใดขั้นตอนหนึ่งเกิดข้อผิดพลาด ต้องใช้ `rollback` เพื่อยกเลิกการเปลี่ยนแปลงทั้งหมด ทำให้ข้อมูลไม่เกิดความไม่สอดคล้องกัน แต่ถ้าทุกขั้นตอนสำเร็จจึงใช้ `commit` เพื่อบันทึกข้อมูลอย่างถาวร

## 3. In-memory Cache กับ Multi-instance Deployment

In-memory cache จะเก็บข้อมูลไว้ในหน่วยความจำของแต่ละ instance ดังนั้นถ้าระบบมีหลาย instance แต่ละ instance จะมี cache ของตัวเอง ทำให้ข้อมูลใน cache อาจไม่เหมือนกัน และเมื่อมีการแก้ไขข้อมูล การล้าง cache ใน instance หนึ่งจะไม่ล้าง cache ของ instance อื่น วิธีที่เหมาะสมกว่าคือใช้ shared cache เช่น Redis เพื่อให้ทุก instance ใช้ cache ชุดเดียวกันและสามารถจัดการข้อมูลได้สอดคล้องกัน

## 4. การจัดการ API Deprecation

เมื่อมีการสร้าง API version ใหม่ เช่น `/api/v2` ควรประกาศให้ผู้ใช้ทราบว่า `/api/v1` จะถูกยกเลิกหรือเลิกสนับสนุนเมื่อใด พร้อมระบุระยะเวลาในการย้ายไปใช้ v2 และควรมีเอกสารหรือคำแนะนำสำหรับการเปลี่ยนแปลง นอกจากนี้อาจใช้ HTTP Header เช่น `Deprecation` หรือ `Sunset` เพื่อแจ้ง Client ว่า API เดิมกำลังจะถูกยกเลิก เพื่อให้ผู้ใช้งานมีเวลาเตรียมตัวและย้ายระบบอย่างเหมาะสม
